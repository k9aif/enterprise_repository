POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=enterprise_repository
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_SCHEMA=enterprise_repository
REPO_PORT=8086

REPO_API_KEY=changeme-generate-with-python3-c-import-secrets-print-secrets-token-urlsafe-32

# Optional — defaults to https://continuum.k9x.ai (the live production
# Continuum) if unset. Override for local dev, e.g. http://localhost:8085.
CONTINUUM_URL=
